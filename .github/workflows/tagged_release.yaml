name: Create tagged release
on:
  workflow_dispatch:

jobs:
  test:
    strategy:
      matrix:
        os: [ ubuntu-latest, windows-latest, macos-latest ]
        node: [ 18 ]
    runs-on: ${{matrix.os}}
    env:
      NODE_TLS_REJECT_UNAUTHORIZED: "0"
      npm_config_strict_ssl: "false"
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v3
      - name: Set up node
        uses: actions/setup-node@v3
        with:
          node-version: ${{matrix.node}}
          registry-url: 'https://registry.npmjs.org'
      - name: Setup pnpm
        uses: pnpm/action-setup@v3.0.0
      - name: Configure registry for @kumar-sambhav scope
        run: |
          printf "@kumar-sambhav:registry=https://registry.npmjs.org/\n//registry.npmjs.org/:_authToken=%s\n" "${NPM_TOKEN}" > .npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      - name: Install
        run: pnpm i
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          npm_config_strict_ssl: "false"
      - name: Compile
        run: npm run compile

  deploy:
    runs-on: ubuntu-latest
    env:
      NODE_TLS_REJECT_UNAUTHORIZED: "0"
      npm_config_strict_ssl: "false"
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v3
      - name: Set up node
        uses: actions/setup-node@v3
        with:
          node-version: 18
          registry-url: 'https://registry.npmjs.org'
      - name: Setup pnpm
        uses: pnpm/action-setup@v3.0.0
      - name: Configure registry for @kumar-sambhav scope
        run: |
          printf "@kumar-sambhav:registry=https://registry.npmjs.org/\n//registry.npmjs.org/:_authToken=%s\n" "${NPM_TOKEN}" > .npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      - name: Install
        run: pnpm i
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          npm_config_strict_ssl: "false"
      - name: Publish tagged release to npm
        run: |
          ./.build/pre-publish.sh
          pnpm run publish:release
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NODE_TLS_REJECT_UNAUTHORIZED: "0"
          npm_config_strict_ssl: "false"
