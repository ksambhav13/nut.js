{
  "name": "@computer-use/provider-interfaces",
  "version": "4.2.0",
  "description": "Public provider interfaces for @computer-use/nut-js",
  "main": "dist/index",
  "typings": "dist/index",
  "scripts": {
    "clean": "rimraf dist",
    "compile": "pnpm run clean && tsc -p .",
    "publish:next": "pnpm publish --tag next --no-git-checks",
    "publish:release": "pnpm publish --no-git-checks",
    "prepublishOnly": "pnpm run compile",
    "typedoc": "typedoc --out docs"
  },
  "keywords": [
    "nut-js",
    "provider"
  ],
  "author": {
    "name": "dry Software UG (haftungsbeschränkt)",
    "email": "info@dry.software",
    "url": "https://dry.software"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@computer-use/shared": "workspace:*"
  },
  "devDependencies": {
    "@computer-use/configs": "workspace:*"
  }
}
